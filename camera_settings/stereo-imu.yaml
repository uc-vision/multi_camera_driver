camera_aliases:
  19056615: left
  19056617: right

cam-imu-offset: 0.030006283033521982

camera_settings:
  device:
    - UserSetSelector:
      - Default
      - ENUM
    - TriggerSource:
      - Line3
      - ENUM
    - TriggerMode:
      - "On"  # needs quotes otherwise is evaluated to false
      - ENUM
    #    TriggerOverlap:
    #      - "ReadOut"
    #      - ENUM
    - FrameCounter:
      - True
      - CHUNK
    - ChunkModeActive:
      - True
      - BOOL
    - ExposureAuto:
      - "Off"
      - ENUM
    - ExposureTime:
      - 5000.0
      - FLOAT
    - GainAuto:
      - "Off"
      - ENUM
  transport_layer:
    - StreamBufferCountMode:
      - Auto
      - ENUM
    - StreamBufferHandlingMode:
      - NewestOnly
      - ENUM
#  Stream:

