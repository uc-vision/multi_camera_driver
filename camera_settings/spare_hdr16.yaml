camera_aliases:
  21264079 : left

master: 21264079

camera_settings:
  device:
    - UserSetSelector:
      - Default
      - ENUM
    - SensorShutterMode:
      - Global
      - ENUM
      
    - PixelFormat:
      - BayerRG12p
      - ENUM

  transport_layer:
    - StreamBufferCountMode:
      - Manual
      - ENUM

    - StreamBufferCountManual:
      - 2
      - INT

    - StreamBufferHandlingMode:
            #      - NewestOnly
      - OldestFirst
      - ENUM
#  Stream:
